name: Trigger auto deployment for repotest1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ hfapp1 ]
    paths:
    - '**'
    - '.github/workflows/repotest1-AutoDeployTrigger-48e0a73b-8aec-4262-bcfc-a68ad70c44cc.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: ca5d1f8ea6ddacr.azurecr.io
          username: ${{ secrets.REPOTEST1_REGISTRY_USERNAME }}
          password: ${{ secrets.REPOTEST1_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: ca5d1f8ea6ddacr.azurecr.io/repotest1:${{ github.sha }}
          file: ./Dockerfile
          context: ./


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.REPOTEST1_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az config set extension.use_dynamic_install=yes_without_prompt
            az containerapp registry set -n repotest1 -g helloworldrg --server ca5d1f8ea6ddacr.azurecr.io --username  ${{ secrets.REPOTEST1_REGISTRY_USERNAME }} --password ${{ secrets.REPOTEST1_REGISTRY_PASSWORD }}
            az containerapp update -n repotest1 -g helloworldrg --image ca5d1f8ea6ddacr.azurecr.io/repotest1:${{ github.sha }}
